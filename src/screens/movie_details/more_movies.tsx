import React, { FC, memo } from 'react';
import ScreenContainer from '../../components/common/screenContainer';
import { View } from 'react-native';

interface props {}

const MoreMovieList: FC<props> = () => {
  return (
    <ScreenContainer>
      <View></View>
    </ScreenContainer>
  );
};

export default MoreMovieList;
