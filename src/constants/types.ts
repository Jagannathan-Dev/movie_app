type leaveInputType = {
  leave_type: string;
  reason: string;
  fromdate: string;
  todate: string;
  day_type: string;
};

type passwdChangeType = {
  currentPassword: string;
  newPassword: string;
  confirmPassword: string;
};
